# 参与开发

从最新 main 创建功能分支，并行工作使用独立 worktree。公开仓库只维护 CLI、Skill 和本地画布，不加入自营网站的认证、数据库或发布配置。

运行 npm ci、npm test、npm run lint。基础 CLI 不需要 npm install；修改安装器时检查独立安装后可运行。界面变更检查桌面和手机，算法变更检查固定样本、字符还原、双向覆盖和真实分数。

提交说明写清问题、最终行为、验证与未测范围。不要提交私人会话、密钥、模型缓存、依赖或安装包。确认演示材料有权公开，不能为他人材料擅自追加许可证。
