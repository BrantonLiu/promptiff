# 参与开发

感谢帮助改进版本比对器。开发环境、数据生成和部署流程分别见 [开发指南](docs/development.md) 与 [部署指南](docs/deployment.md)。

## 修改与验证

1. 从最新主分支创建功能分支，先明确本次修改的用户行为或算法问题。
2. 只修改相关文件。并行工作可用 `git worktree`，让各分支在独立目录运行；不要让两个任务同时写同一个工作目录。
3. 按开发指南运行与改动相关的验证，并完成类型检查、lint 和构建。
4. 提交时说明解决的问题、最终行为、验证结果。修改视觉样式请附桌面和手机视图；修改算法请附固定样本与指标差异。

不要把生成的 `dist/`、本地数据库状态、依赖目录、环境变量或访问凭据提交到仓库。环境模板仅保留占位符。实际文章、反馈和账号信息也不应作为测试夹具公开。

## 值得保留的行为

- 四种视图与四个版本均可通过 URL 到达，浏览器前进后退可用。
- 同一原文可由分词和审阅数据无损恢复。不要为修饰排版改变用于比较的字符。
- 分词算法与 embedding 分别计算；视觉羽化不能篡改分数。
- 反馈等待四种模式均在前台被使用，且真实前台时间严格大于 10 分钟。后台标签页、失焦窗口与睡眠时间不应增加计时。
- Supabase 高权限密钥只能用于服务端；数据库变更通过可审查的迁移文件提交。

## 内容与授权

替换或补充样本前，请确认有权公开原文、图片和衍生版本。代码采用 [MIT 许可证](LICENSE)，文章正文、个人叙述、示例图片与第三方素材不在该许可范围内。不要把演示数据与代码授权混为一谈，也不要擅自为其他作者添加许可证。
