# 发布验证

用户要求：后续每次发布到生产之前，必须实际走查基础按钮。

- 在生产构建的本地预览中点击测试；不能仅凭 HTTP 200、页面文案匹配、构建或单元测试通过就发布。
- 首页顶部与首屏的“查看比对示例”、底部入口和四种功能入口，都要验证点击后 URL 与页面内容实际变化。
- 走查模式切换、改写程度、隐藏删除、只看变化、同步滚动、匹配详情与候选定位、弹窗开关、返回首页、隐私页、复制链接和下载。
- 至少检查桌面与手机宽度；查看浏览器控制台是否有阻断交互的错误。
- 发布后在生产页再次实际点击主要入口和模式切换。发现问题先修复，不能把未完成的检查描述为已通过。
- Google 授权和反馈提交若不在本轮测试范围，明确记录边界。不能用读取配置代替完整登录验证，也不要为了测试降低生产反馈门槛。
- 每次在发布记录中写明测试环境、实际点击项、结果和未测项。详细清单见 `docs/ui-smoke-check.md`。

# 页面导航

跨页面导航使用原生 `<a href>`。当前 vinext 生产包曾在 `next/link` 的 RSC 预取和导航时抛出 TypeError，拦截链接后无法跳转。不要在没有生产构建点击回归测试的情况下重新引入客户端 Link。
