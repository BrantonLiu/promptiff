# 仓库边界

本仓库公开维护 CLI、Skill、静态画布、示例、测试和用户文档。网站、登录反馈后端和生产配置在独立 private 的 wordiff-web 仓库维护。不要在这里加入 Worker/Supabase 配置、生产项目标识、真实会话或密钥。

# 开发验证

- 修改前检查分支与工作树，避免多个任务同时改共享 checkout。
- 基础 CLI 无需 npm 依赖可运行；Skill 安装后不能依赖源仓库位置。
- 共享画布只在这里修改；网站消费锁定的 Git 提交，不在两处复制维护。
- 运行 npm test、npm run lint；打包或路径变化时验证独立安装。
- 跨页面导航保持原生 <a href>。
- 远程推理仍需用户显式选择，会话与凭据不得入库。

生产按钮走查要求已完整迁入网站仓库。本仓库没有生产部署命令，不要在旧历史分支继续发布网站。
